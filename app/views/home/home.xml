<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded" class="page">
    <ScrollView>
        <StackLayout class="home">
            <Label text="Facebook"></Label>
            <Button tap="facebook" text="Iniciar Cuenta en Facebook" class="btn btn-primary"></Button>
            <Label text="Calculadora"></Label>
            <Button tap="suma" text="Suma" class="btn btn-primary"></Button>
            <Button tap="resta" text="Resta" class="btn btn-primary"></Button>
            <Button tap="multiplicacion" text="Multiplicación" class="btn btn-primary"></Button>
            <Label text="Fotografias"></Label>
            <Button tap="photo" text="foto" class="btn btn-primary"></Button>
            <Button tap="addphoto" text="Agregar Foto" class="btn btn-primary"></Button>
            <Button tap="gallery" text="Mostrar Fotos" class="btn btn-primary"></Button>
            <Button tap="ratephoto" text="Actualizar Fotos" class="btn btn-primary"></Button>
        </StackLayout>    
    </ScrollView>
</Page>